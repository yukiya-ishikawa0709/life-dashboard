# life-dashboard

日常生活サポートツール(スケジュール管理・家計簿・筋トレ記録・勉強記録を統合)。詳細要件は [requirements.md](requirements.md) を参照。

個人利用・学習目的のプロジェクト。エンジニアとしてのステップアップ(Rust/システムプログラミング領域の習得)も目的の一つ。

## 技術スタック

| 項目 | 内容 |
|---|---|
| フレームワーク | Tauri 2.0(Rust + Web技術) |
| フロントエンド | React + TypeScript |
| バックエンド/ロジック | Rust |
| データ保存 | ローカルDB(SQLite想定) |
| 対応OS(第一段階) | Mac / Windows(デスクトップ) |
| 対応OS(第二段階) | iOS / Android(スマホ、Tauriのモバイル対応で拡張予定) |

## 設計方針

- **ビジネスロジックとUIの分離**: 将来のスマホ版展開を見据え、ドメインロジックはRust側に寄せ、Reactは表示・操作に専念させる。
- **日付ベースの共通データ構造**: スケジュール・家計簿・筋トレ・勉強の各記録は日付を軸にした共通基盤の上に構築する。
- **オフラインファースト**: 外部通信必須の機能は作らない。すべてローカルDBで完結させる。
- **認証・マルチユーザー非対応**: 個人利用前提のため、認証機構は現時点でスコープ外。

## 開発ロードマップ

共通基盤 → 機能追加、の順で段階的に開発する。

1. 共通基盤(ローカルDB設計、Tauri + React雛形、画面遷移・レイアウトの土台)
2. スケジュール管理(MVP軸)
3. 家計簿
4. 筋トレ記録
5. 勉強記録

各機能の詳細要件は個別ヒアリングで確定し、[requirements.md](requirements.md) に追記していく。実装前に該当機能のTBD項目が埋まっているか確認すること。

## 開発スタイル

- 要件定義・設計・テストはユーザー自身が主導する。AIは実装支援として活用する。
- 機能追加時は、まず該当機能の要件が requirements.md 上で確定しているかを確認してから着手する。

## 開発ワークフロー(GitHub Issue駆動)

タスクは以下の流れで進める。実装作業を依頼されたら、コード変更に入る前に必ず1〜2を行うこと。

1. **Issue作成**: `gh issue create` で対応するテンプレート(機能タスク/バグ報告)を使って作成する。ロードマップの段階が明確な場合は `stage:*` ラベルを付与する。Notionのタスク一覧(下記)に対応するタスクがある場合は、Issueタイトルにそのタスク番号を含める(例: `[06] スケジュール管理の詳細要件ヒアリング`)。
2. **ブランチ作成**: Issue番号を使い `<issue番号>-短い説明`(例: `12-schedule-list-view`)の形式でブランチを切り、そこで作業する。mainに直接コミットしない。
3. **実装・コミット**: ブランチ上で実装を進める。
4. **PR作成**: 作業が完了したら `gh pr create` でPRを作成し、本文に `Closes #<issue番号>` を含めてIssueと紐付ける。マージはユーザーが行う(このリポジトリでは無断でmainへのマージ・force pushは行わない)。

## 作業記録・学習ログ(Notion)

Rust/Tauriを未経験の状態から学びながら進めているため、タスク(Issue)ごとの作業内容・学びをNotion(「作業進捗」データベース)のタスクページに記録し、学習を蓄積していく。

### タスク一覧(マスタ)

- 開発ロードマップ全体は、Notion「作業進捗」データベースに `01`, `02`, ... の通し番号付きタスクとして事前に分解済み。各行は `ステータス`(`To do` / `Doing` / `Done`)を持つ。
- 品質維持のため、**1セッション = 1タスク**を原則とする。複数タスクを同一セッションでまたがって進めない。次のタスクは新しいセッションで着手する。

### セッションの始め方

ユーザーはタスク一覧のタイトル(または番号、例:「06」)を伝えるだけで着手できる。それを受けたら、コード変更に入る前に以下を行う。

1. Notion「作業進捗」DBから該当タスクページを検索・取得し、概要・目的、および前後のタスクページの「次にやること」「積み残し・今後の検討事項」を確認する。
2. requirements.md の該当TBD項目を確認する。
   - タスク自体が要件ヒアリング(例:「◯◯の詳細要件ヒアリング→requirements.md追記」)であれば、その場でユーザーと対話して要件を確定させる。
   - それ以外の実装系タスクで、参照すべきTBD項目が未確定な場合は、実装に入らずユーザーに確認を促す。
3. Notionのステータスを `Doing` に更新する。
4. 上記「開発ワークフロー」に従い、Issue作成 → ブランチ作成 → 実装 → PR作成を行う。
5. 作業が完了したら、Notionタスクページを次の「タスクページの構成(雛形)」に沿って埋め、ステータスを `Done` に更新する。

### タスクページの構成(雛形)

新しいタスクページを作る際は、以下の見出し構成を踏襲する。

1. 概要・目的
2. やったこと(作業ログ)
3. 基礎知識 — そのタスクで新しく学んだ技術知識をまとめる欄。前提知識ゼロから始めているため、タスクを重ねるごとにこの欄で知識を積み上げていく。初学者にも分かる粒度で書く。
4. つまずいたこと・教訓
5. 参考リンク(Issue/PR)
6. 次にやること
7. 積み残し・今後の検討事項

### 運用ルール

- 「作業進捗」DBに存在しない新規タスクが発生した場合は、直近のタスクページを複製してこの構成を踏襲し、DBに追加する。
